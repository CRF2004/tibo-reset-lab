# Codex 特殊重置前瞻研究协议 v1（冻结）

版本：1.0  
冻结时间：2026-07-28T07:54:34Z  
前瞻主分析起点：首个有效 `schedule_class=scheduled` 的 17:00 UTC 签发。  
状态：`FROZEN FOR PROSPECTIVE EVALUATION`

## 1. 透明性声明

这不是对全部历史分析的事前注册。冻结前已经观察到：

- 41 条历史金标准公告；
- 日级和 6 小时 M0–M3-lite 回测结果；
- M2 在两种粒度上具有最低历史 Brier；
- M3-lite 没有显示稳定的历史增量；
- 事故滞后与变量组消融结果；
- 一份有效 bootstrap 三模型预测。

上述结果属于模型开发和探索阶段。只有本协议冻结后、`scheduled` 类型且未被排除的
预测属于确认性前瞻样本。

## 2. 研究对象和结果

研究对象是公开的组织行动，不是个人心理或人格。

主结果 `announcement_event`：

1. 由 `thsottiaux` 或 OpenAI 官方账号公开；
2. 明确承诺、正在执行或声称已执行 Codex 特殊用量重置；
3. 可与自然的个人 5 小时或周窗口刷新区分；
4. 有原始 X/官方来源或可审计存档；
5. 通过既有 gold 标注规则。

后台实际到账 `reset_applied_event` 是独立的次要研究结果，不替代公告结果。

## 3. 签发时点与边界

- 固定签发时点：每日 `17:00:00 UTC`；
- 主 horizon：24 小时；
- 次要 horizon：168 小时；
- 结果窗口：`(issued_at, window_end]`；
- 恰好发生在签发时刻的公告不属于未来结果；
- 恰好发生在终点的公告属于结果；
- 只用签发前已经结束的完整日训练窗口；
- 不允许事后补签或把 bootstrap 改成 scheduled。

若采集、核验或完整性门禁失败，当日写入 `missed_forecast_runs.csv`，不得回填。

## 4. 冻结模型

### M0

Beta(1,1) 平滑的 expanding historical daily event rate。

### M2

L2 logistic regression，`C=0.25`，`solver=lbfgs`。特征：

- `log1p(days_since_last_announcement)`；
- 太平洋时区星期的正余弦；
- 周末指示；
- 月份正余弦；
- 2026-06-11 banked-reset 制度指示。

连续/周期变量只在每个训练窗内标准化，二元变量直通。

### M3-lite

与 M2 相同的估计器设置，但以 renewal 特征加预测安全官方事故特征：

- 过去 72 小时是否有可见官方事故；
- 事故是否仍在进行；
- 是否在过去 48 小时解决；
- 最大事件强度；
- 最大组织注意力；
- 最新情境年龄。

公告文本、事后原因、未来解决时间、里程碑和发布标签不得进入 M3-lite。

任务 7 主序列期间不改变上述特征、正则参数或预处理。任何新模型必须使用新版本号
作为 challenger 并行签发，不得替换或回写旧预测。

## 5. 预测量

每个模型同时锁定：

- `p_24h`：签发后 24 小时内至少一个公告的概率；
- `p_7d`：七个逐日 hazard 的非事件概率乘积之补数。

7 日投影条件是中间没有新公告改变 renewal state。该近似固定保留，不能根据表现
事后更换。按重置类型的条件分布不属于本协议主评分。

## 6. 数据完整性门禁

每日预检必须确认：

1. 发现层 feed 不陈旧且抓取时间距预检不超过 30 分钟；
2. 当前 signal 的原始 X ID 已进入 accepted gold；
3. 官方状态采集无结构错误；
4. CSV、UTC、枚举和外键验证通过；
5. 当次输入与脚本 SHA-256 已写入锁文件。

独立追踪器只用于发现，不作为 gold 的最终官方证据。发现未知 signal 时创建候选并
阻止签发，等待原始 X 核验与 LLM 标注/人工抽查。

## 7. 排除、缺失和修订

- `bootstrap` 不进入 scheduled 主分析；
- `forecast_exclusions_v1.csv` 中的运行全部排除；
- 错误时间、未来时间、重复签发和数据门禁失败均不得评分；
- 漏签是研究结果的一部分，报告缺失比例和原因；
- 已锁定概率永不修改；
- 到期结果写入独立追加表；
- 若后来发现漏标，保留原评分并另建修订记录及敏感性结果。

## 8. 主假设和比较

- P1：M2 的 24h Brier 低于 M0；
- P2：M3-lite 的 24h Brier 低于 M2；
- P3：M2 的 24h Log Loss 低于 M0；
- P4：M3-lite 的 24h Log Loss 低于 M2。

P1 是主要预测比较；P2 是理论特征增量检验。168h、PR-AUC、校准、按制度分层及
到账结果均为次要或探索性分析。

## 9. 评价

- 主指标：24h Brier Score；
- 关键次指标：24h Log Loss；
- 其他指标：168h Brier/Log Loss、校准截距与斜率、PR-AUC、sharpness；
- 模型必须使用完全相同的 scheduled 预测时点；
- 不确定性使用 paired 7 日 block bootstrap，固定 2000 次；
- 同时报告原始差值、相对 skill 和 95% percentile interval；
- 不以单次猜中、单一最大事故或未注册子组支持结论。

## 10. 停止规则

达到以下任一条件后执行第一次正式比较：

1. 60 个有效 scheduled 日级签发；
2. 24h 窗口累计 15 个前瞻正例；
3. 连续运行 90 天。

停止条件按最先到达者触发。触发前只报告运行完整性和描述性累计分数，不做胜负宣告。

## 11. 兑现研究

到账报告按成功、延迟、遗漏、只改计时器、hard/banked 机制不符分类。社区报告是
自选择样本，不能估计总体失败率。只有精确发布时间进入延迟统计；日期级或仅有顺序
的信息只进入模式计数。目标样本量 20–30 是资料覆盖目标，不是显著性停止规则。

## 12. 伦理与解释

- 不推断个人内心动机、疾病、人格或私人生活；
- 不鼓励刷帖、施压或制造事故信号；
- 只使用公开职业行为与公开产品事件；
- 概率不是确定判断；
- 预测项目若可能影响被预测行为，应记录潜在干预并做敏感性披露。

## 13. 版本治理

本文件冻结后不得静默编辑。修正拼写不改变语义时也应发布新版本并记录 diff。
模型、结果、窗口、停止规则或排除标准的任何语义变化必须创建 v2，且 v1 继续保留。
