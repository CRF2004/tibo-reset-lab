# 预注册草案 v0.1

状态：`DRAFT — 必须在完整历史结果揭示和主分析前冻结`

创建日期：2026-07-28

## 1. 研究问题

仅使用预测时点已经公开且已被采集的信息：

1. 能否预测未来 24 小时或 7 天内首次出现 Codex 特殊用量重置公告？
2. 理论驱动模型 M3 能否在严格滚动回测中优于 M0–M2？
3. 官方确认、事件强度和组织注意力是否与补偿型公告的短期 hazard 存在增量关联？

研究不对 Tibo 的内心动机、人格或私人行为作推断。

## 2. 观测单位与时间

- 主时间粒度：UTC 自然日；日级管线稳定后，6 小时粒度作为扩展分析。
- 正式预测时点：每日 17:00 UTC（待冻结）。
- 24 小时窗口：`(issued_at, issued_at + 24h]`。
- 7 天窗口：`(issued_at, issued_at + 168h]`。
- 边界规则：恰好发生在 `issued_at` 的事件不属于未来窗口；恰好发生在窗口终点的事件属于该窗口。
- 信息可用时间使用 `first_observed_at_utc`，不得以更早的发布时间替代。

## 3. 结果定义

主结果 `announcement_event` 需同时满足：

1. Tibo 或 OpenAI 官方账号公开明确表示特殊重置已发生、正在发生或承诺发生；
2. 行动可与用户个人自然窗口刷新区分；
3. 有原始来源或可审计存档；
4. 时间可确定到不超过一个主时间片。

公告状态：`promised | in_progress | claimed_done`。

重置类型：

`hard_global | banked_credit | targeted_or_conditional | extension_or_multiplier | promise_only`

原因类型：

`incident_compensation | milestone_celebration | launch_promotion | community_response | mixed_or_unclear`

后台到账 `reset_applied_event` 是独立结果，不用于替代公告结果。

## 4. 排除规则

- 正常的 5 小时、每日、每周或其他自然刷新；
- 只有用户猜测、没有官方公告或可靠多源到账证据；
- 无法确定是否属于 Codex 用量限制的泛化“reset”；
- 重复转发同一个公告；
- 公告发生后才出现的文本不得作为该公告的预测特征。

模糊候选保留在 interim 表，状态记为 `uncertain`，不强制归类为正例。

## 5. 制度时期

- `pre_banked_reset`
- `banked_reset_rollout`
- `post_banked_reset`

确切切点须依据首个公开产品证据确定并在主标注揭示前冻结。制度切点不得根据模型成绩调整。

## 6. 主假设

- H1：官方确认的负向产品事件提高 48 小时内补偿型公告 hazard。
- H2：事件强度提高公告 hazard。
- H3：高组织注意力增强社区信号与公告之间的关联。
- H4：`事件强度 × 高注意力` 的效应为正。
- H5：修复确认后的短窗口比纯调查早期更易出现补偿公告。
- H6：里程碑和重大发布提高庆祝/推广型公告 hazard。

探索性：H7 社区阈值；H8 制度漂移；H9 重置后的短期自激或抑制。

总预测标准 H10：M3 在相同预测时点上的滚动 Brier Score 和 Log Loss 优于 M0–M2，且改善不由单一事件主导。

## 7. 事件强度与注意力

`event_strength = novelty + disruption + criticality`，每维 0–3，总分 0–9。

两名标注者独立评分，标注时不得查看后续公告结果。报告逐项分歧、加权 Cohen's kappa（有序分类）或 ICC（总分）。裁决不覆盖原始评分。

`attention_state` 为 0–5：

0 无公开注意；1 社区聚合；2 员工接触；3 正式确认；4 负责人持续关注；5 解决与补偿进入公开议程。

注意：包含明确重置承诺的文本属于结果开始，不能编码成结果前注意力特征。

## 8. 预注册社区阈值

主分析仅比较以下两个阈值（数值待 pilot 数据揭示前冻结）：

1. 6 小时内独立报告者达到固定人数；
2. 同一事件在至少两个独立平台出现且至少有一次员工/官方接触。

阈值不得在全样本上搜索。任何训练窗内学习的阈值只作为探索分析。

## 9. 固定模型

- M0：截至预测时点的历史事件率；制度分层版本作为敏感性分析。
- M1：仅使用距上次公告时间的 renewal 基线。
- M2：M1 + 太平洋时区小时、星期、周末和近期公开活动。
- M3：离散时间 Bayesian complementary-log-log hazard；只使用事件强度、注意力、官方负反馈、事故阶段、里程碑/发布、预注册社区阈值、制度时期，以及 `strength × attention` 和 `negative_feedback × official_confirmation`。

在 M3 前不得用完整数据依据表现筛选变量。标准化系数的主先验为零中心弱信息先验；具体实现和版本在首次拟合前补充并冻结。

## 10. 评价与回测

- expanding-window 滚动起点；不使用随机切分；
- 所有模型共享完全相同的预测时点、数据截止和标签；
- 主指标：Brier Score、Log Loss；
- 辅助：Brier Skill Score、校准截距/斜率、PR-AUC、sharpness；
- 不确定性：按周或事件周期 block bootstrap；
- 7 天窗口重叠会明确报告，不使用独立样本标准误；
- 敏感性：24h/48h/7d、排除最大故障、排除庆祝事件、按制度时期、公告者范围。

## 11. 停止与报告规则

若金标准正例或独立事件周期不足以支撑 M3，将只发布描述性结果和 M0–M2，不以复杂模型制造结论。论文触发依据累计预测周期，而不是某次“猜中”。

## 12. 尚待冻结

- 历史观察起点；
- 正式预测时点；
- banked reset 制度切点；
- 两个社区阈值的具体数值；
- M1 等待时间函数；
- M3 先验和最小正例要求；
- “Tibo 公告”与“官方账号公告”主/敏感性口径。

