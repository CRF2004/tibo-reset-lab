# 任务 8、10、9 推进记录

执行日期：2026-07-28。

## 任务 8：持续运行与数据完整性

已建立三阶段每日任务：

1. 北京时间 00:50（16:50 UTC）预检发现 feed；
2. 北京时间 01:00（17:00 UTC）运行完整性门禁、状态采集、特征重建、验证和正式签发；
3. 北京时间 01:10（17:10 UTC）追加所有已到期结果。

Windows Task Scheduler 中已安装：

- `TiboResearchPreflight`
- `TiboResearchForecast`
- `TiboResearchScore`

预检任务已由 Task Scheduler 手工触发验证，返回码为 0。最新 signal
`2081940052154933696` 已存在于 accepted gold。

自动化不会把独立追踪器当官方证据。若 feed 陈旧、未知 signal 尚未完成原始 X 核验、
预检不匹配、验证失败或重复签发，系统拒绝签发并把缺失写入
`missed_forecast_runs.csv`，不得事后回填。运行过程、快照与日志全部保留。

边界：任务依赖 Windows 用户会话可运行的计划任务、网络连接以及独立发现 feed。
这能防止“静默断档”，但不能保证外部服务永不宕机；外部失败会转化为显式、可审计
的 missed run。

## 任务 10：冻结研究设计

已冻结 `preregistration_v1_frozen.md`，SHA-256：

`de216beb5dbbf1dbc77ae863f2628b1d750c55f9c7e52f6efed93b0b39d949cb`

协议明确披露冻结前已查看全部历史回测和消融，因此只把未来有效 scheduled 签发视为
确认性证据。主比较为 M2 对 M0 的 24h Brier；M3-lite 对 M2 是理论增量检验。模型、
边界、排除、缺失、停止规则和修订治理均已冻结。

## 任务 9：兑现证据扩充

公开个案从 4 条扩充到 20 条，并为每条保存原文摘要、中文翻译、账户/客户端信息、
时间精度和证据类型。新增材料覆盖：

- 从 0%、1%、2% 或其他余量恢复到 100%；
- 明确未到账；
- 只推进计时器但未恢复额度；
- 周窗口日期被推迟；
- hard reset 覆盖刚使用的 banked reset；
- hard reset 未作为可叠加 banked credit 发放。

只有 1 条报告具有精确时间并可进入延迟统计；其余只用于模式分类。社区报告仍是
自选择样本，20 条不是总体失败率的估计样本。

## 验证

- 15 张受控 CSV 表、255 行通过结构、UTC、枚举与外键验证；
- 冻结协议 SHA-256 复算通过；
- 自动化脚本、签发脚本、评分脚本和兑现分析均通过语法检查；
- Windows 预检计划任务实跑成功。
